// Package types contains exported types shared between the CLI client library
// and external tooling. These types mirror the protobuf message definitions
// but are stable, independent of the generated protobuf code.
//
// License: Apache 2.0
package types
