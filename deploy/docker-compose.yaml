version: "3.9"

# Local development environment for jitsudo.
# Starts: jitsudod control plane, PostgreSQL, and dex (mock OIDC provider).
#
# Usage:
#   make docker-up    # start all services (full Docker stack)
#   make docker-down  # stop
#   make docker-logs  # tail logs
#
# NOTE: For local development it is easier to run jitsudod on the host:
#   make dev-deps     # start only postgres + dex
#   make dev-server   # run jitsudod on the host
#
# This avoids an OIDC issuer URL mismatch: dex issues tokens with
# iss=http://localhost:5556/dex but jitsudod inside Docker cannot resolve
# localhost:5556. If you need the full Docker stack, you must update
# JITSUDOD_OIDC_ISSUER below AND dex-config.yaml issuer to match.

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: jitsudo
      POSTGRES_PASSWORD: jitsudo
      POSTGRES_DB: jitsudo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jitsudo"]
      interval: 5s
      timeout: 5s
      retries: 5

  dex:
    image: ghcr.io/dexidp/dex:v2.41.1
    command: ["dex", "serve", "/etc/dex/config.yaml"]
    ports:
      - "5556:5556"
    volumes:
      - ./dex-config.yaml:/etc/dex/config.yaml:ro
    depends_on:
      postgres:
        condition: service_healthy

  jitsudod:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.jitsudod
    environment:
      JITSUDOD_HTTP_ADDR: ":8080"
      JITSUDOD_GRPC_ADDR: ":8443"
      JITSUDOD_DATABASE_URL: "postgres://jitsudo:jitsudo@postgres:5432/jitsudo?sslmode=disable"
      JITSUDOD_OIDC_ISSUER: "http://localhost:5556/dex"
      JITSUDOD_OIDC_DISCOVERY_URL: "http://dex:5556/dex"
      JITSUDOD_OIDC_CLIENT_ID: "jitsudo-cli"
    ports:
      - "8080:8080"
      - "8443:8443"
    depends_on:
      postgres:
        condition: service_healthy
      dex:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
